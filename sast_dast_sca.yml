# Run SonarQube
docker run -d -p 9000:9000 --name sonarqube sonarqube:9.9-community

# use SonarQube CLI
sonar-scanner -Dsonar.host.url=http://localhost:9000 -Dsonar.login=<TOKEN>

# For token
setting -> myaccount -> security -> create token


# SCA --- 

# create virtual environment
python3 -m venv .venv

pip install pip-audit
pip audit -r requirements.txt

OWASP juice shop

# Install OWASP ZAP
docker run -d --name juice-shop -p 3000:3000 bkimminich/juice-shop

# Install zap using flow

# Access report on browser
file:///path/html 

# Run OWASP with GUI
docker run -it -p 8080:8080 ghcr.io/zaproxy/zaproxy:stable zap-webswing.sh
